# Serving-time stack — Streamlit app + FastAPI (Phase B / Docker image)
# Lean on purpose: no scikit-learn training deps, no scipy, no jupyter
# Install with: pip install -r requirements-api.txt

streamlit>=1.37.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# Inference + display only (model objects already pickled — no training)
joblib>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.18.0

# Monitoring
prometheus-client>=0.19.0

# Config
python-dotenv>=1.0.0
