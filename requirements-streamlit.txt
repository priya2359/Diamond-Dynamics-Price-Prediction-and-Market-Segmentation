# Streamlit serving stack (Phase 2A / Docker image)
# No scikit-learn/xgboost/joblib -- the app calls the FastAPI service over
# HTTP for all predictions (see streamlit_app/utils.py, FASTAPI_URL).
# Install with: pip install -r requirements-streamlit.txt

streamlit>=1.37.0
requests>=2.31.0

# Visual Insights tab reads static artifacts (pca_2d.csv, model_comparison.csv,
# cluster_profiles.json) directly -- no API round-trip needed for these.
pandas>=2.0.0
plotly>=5.18.0

# Config
python-dotenv>=1.0.0
