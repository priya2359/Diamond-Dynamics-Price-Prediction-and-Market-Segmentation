# Development tooling — linting, testing, formatting (NOT shipped to Docker image)
# Install with: pip install -r requirements.txt -r requirements-dev.txt

pytest==9.0.3
pytest-cov==7.1.0
flake8==7.3.0
black==26.5.1
mypy==2.1.0
pre-commit==4.6.0

# Section 11 -- FastAPI TestClient (api/main.py tests)
httpx==0.28.1

# Section 9 -- DBSCAN eps selection (KneeLocator), notebook-only analysis
kneed==0.8.6
