# FastAPI serving stack (Phase 2A / Docker image)
# Streamlit no longer loads models directly (calls this API over HTTP) --
# this is the only image that needs scikit-learn/xgboost to unpickle and
# run the saved Pipelines.
# Install with: pip install -r requirements-fastapi.txt

fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4

# Model loading + inference (objects already pickled -- no training)
joblib==1.5.3
scikit-learn==1.9.0
xgboost==3.2.0
pandas==2.3.3
numpy==2.4.6

# Monitoring
prometheus-client==0.25.0
scipy==1.17.1

# Rate limiting
slowapi==0.1.10

# Config
python-dotenv==1.2.2
